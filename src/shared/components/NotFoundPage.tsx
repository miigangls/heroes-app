import { PagePlaceholder } from './PagePlaceholder'

export const NotFoundPage = () => {
  return (
    <PagePlaceholder
      title="404"
      description="No encontramos la ruta solicitada."
    />
  )
}
